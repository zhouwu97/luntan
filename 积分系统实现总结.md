# 积分系统统一账本实现总结

## 实现的功能

### 1. 每日积分共享额度
- **点赞帖子** 和 **发布帖子** 两个入口共享每日 +1 额度
- 幂等 key 格式：`daily:YYYY-MM-DD:user:用户ID`
- 按 `Asia/Shanghai` 时区计算业务日期
- 每天 00:00 自动重置，用户可以重新获得 +1

### 2. 推荐积分按帖子终身奖励
- 管理员首次推荐帖子时，作者获得 +20 积分
- 幂等 key 格式：`post:recommend:帖子ID`
- 取消推荐不扣积分
- 同一帖子再次推荐不再奖励

### 3. 核心函数

#### `awardDailyPointTx`
```go
func awardDailyPointTx(ctx context.Context, tx *sql.Tx, userID, source, reason string, delta int64) error
```
- 统一处理每日积分奖励
- 自动生成按日期+用户的幂等 key
- 查询当前业务日期（Asia/Shanghai）

#### `awardPointsTx`
```go
func awardPointsTx(ctx context.Context, tx *sql.Tx, userID, source, reason, idempotencyKey string, delta, dailyLimit int64) error
```
- 通用积分发放函数
- 支持自定义幂等 key（用于推荐积分）
- 事务保证流水和余额一致性

### 4. 积分规则

| 行为 | 积分 | 限制 |
|-----|------|------|
| 点赞帖子/评论 | +1 | 与发帖共享每日额度 |
| 发布帖子 | +1 | 与点赞共享每日额度 |
| 帖子被推荐 | +20 | 每个帖子终身只奖励一次 |
| 取消推荐 | 0 | 不扣积分 |
| 再次推荐同一帖子 | 0 | 防止反复推荐刷积分 |

### 5. 修改的文件

#### `server/internal/api/point_rewards.go`
- 新增 `awardDailyPointTx` 函数
- 查询 `Asia/Shanghai` 时区日期

#### `server/internal/api/interactions.go`
- 点赞帖子改用 `awardDailyPointTx`
- 点赞评论改用 `awardDailyPointTx`

#### `server/internal/api/post.go`
- 发帖改用 `awardDailyPointTx`

#### `server/internal/api/recommendations.go`
- 推荐保持使用 `awardPointsTx`，幂等 key 为 `post:recommend:帖子ID`

### 6. 测试覆盖

#### `point_rewards_daily_test.go`
- `TestDailyPointCompetition` - 验证点赞和发帖竞争同一份每日额度
- `TestRecommendationBonusIsIdempotentPerPost` - 验证推荐积分按帖子幂等
- `TestDailyPointReset` - 验证第二天可以重新获得 +1

#### `api_test.go`
- 更新 `TestCreatePostWritesRevisionAndIsIdempotentByUserKey` - 发帖测试适配每日积分
- 更新 `TestPostLikeIsIdempotentAndUpdatesCountOnlyAfterNewRelation` - 点赞测试适配每日积分

## 设计亮点

1. **统一收口**：所有积分修改通过 `awardDailyPointTx` 或 `awardPointsTx`，禁止业务代码直接修改 `users.points_balance`

2. **幂等保证**：使用 `point_transactions.idempotency_key` 唯一约束防止重复发放

3. **事务一致性**：流水插入和余额更新在同一事务中，保证数据一致性

4. **时区正确**：按 `Asia/Shanghai` 时区计算业务日期，避免 UTC 日期错误

5. **扩展性强**：未来增加签到、精华、活动奖励等，只需调用 `awardDailyPointTx` 或 `awardPointsTx`

## 验证结果

所有新增测试通过：
- ✅ `TestDailyPointCompetition`
- ✅ `TestRecommendationBonusIsIdempotentPerPost`
- ✅ `TestDailyPointReset`
- ✅ `TestCreatePostWritesRevisionAndIsIdempotentByUserKey`
- ✅ `TestPostLikeIsIdempotentAndUpdatesCountOnlyAfterNewRelation`
