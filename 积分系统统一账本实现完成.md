# 积分系统统一账本实现完成

## 实现总结

已成功实现统一积分账本系统，将点赞、发帖、推荐的积分发放统一收口，确保幂等性和事务一致性。

### 核心改动

#### 1. 新增 `awardDailyPointTx` 函数
```go
// 每日积分统一入口，自动生成按日期+用户的幂等 key
func awardDailyPointTx(ctx context.Context, tx *sql.Tx, userID, source, reason string, delta int64) error
```

- 查询 `Asia/Shanghai` 时区的当前业务日期
- 生成幂等 key：`daily:YYYY-MM-DD:user:用户ID`
- 点赞和发帖共享每日 +1 额度

#### 2. 业务集成

**点赞帖子/评论**（`interactions.go`）
```go
// 点赞奖励与发帖奖励共享每日 +1 额度。
if err := awardDailyPointTx(r.Context(), tx, user.ID, "like", "点赞帖子", s.pointRewards.LikeCreate); err != nil {
```

**发布帖子**（`post.go`）
```go
// 发帖奖励与点赞奖励共享每日 +1 额度。
if err := awardDailyPointTx(r.Context(), tx, user.ID, "post", "发布帖子", s.pointRewards.PostCreate); err != nil {
```

**推荐帖子**（`recommendations.go`）
```go
// 保持原有实现，幂等 key 为 post:recommend:帖子ID
if err := awardPointsTx(r.Context(), tx, authorID, "recommend", "帖子被推荐", "post:recommend:"+postID, s.pointRewards.RecommendationBonus, 0); err != nil {
```

### 积分规则

| 行为 | 积分 | 幂等 Key | 说明 |
|-----|------|---------|------|
| 点赞帖子 | +1 | `daily:日期:user:用户ID` | 与发帖共享每日额度 |
| 点赞评论 | +1 | `daily:日期:user:用户ID` | 与发帖共享每日额度 |
| 发布帖子 | +1 | `daily:日期:user:用户ID` | 与点赞共享每日额度 |
| 推荐帖子 | +20 | `post:recommend:帖子ID` | 每个帖子终身只奖励一次 |
| 取消推荐 | 0 | - | 不扣积分 |
| 再次推荐 | 0 | - | 幂等保护，不重复发放 |

### 测试覆盖

新增测试文件 `point_rewards_daily_test.go`：

1. **TestDailyPointCompetition** - 验证点赞和发帖竞争同一份每日额度
   - 第一次点赞 → +1
   - 第二次点赞 → +0（幂等）
   - 发帖 → +0（同一天已拿过）

2. **TestRecommendationBonusIsIdempotentPerPost** - 验证推荐积分按帖子幂等
   - 首次推荐 → +20
   - 取消推荐 → 不操作积分
   - 再次推荐 → +0（幂等）

3. **TestDailyPointReset** - 验证第二天可以重新获得 +1
   - 第一天点赞 → +1
   - 第二天点赞 → +1（新的日期 key）

更新已有测试：
- `TestCreatePostWritesRevisionAndIsIdempotentByUserKey` - 适配每日积分 mock
- `TestPostLikeIsIdempotentAndUpdatesCountOnlyAfterNewRelation` - 适配每日积分 mock

### 验证结果

```bash
✅ go test ./internal/api
✅ go build ./...
```

所有测试通过，编译成功。

## 设计优势

1. **统一收口** - 所有积分修改通过 `awardDailyPointTx` 或 `awardPointsTx`，禁止业务代码直接修改余额
2. **幂等保证** - 使用 `point_transactions.idempotency_key` 唯一约束防止重复发放
3. **事务一致性** - 流水插入和余额更新在同一事务中，保证数据一致性
4. **时区正确** - 按 `Asia/Shanghai` 时区计算业务日期，避免 UTC 日期问题
5. **扩展性强** - 未来增加签到、精华、活动奖励等，直接复用 `awardDailyPointTx` 或 `awardPointsTx`

## 后续建议

### 1. 前端体验优化
建议接口返回 `points_awarded` 字段：
```json
{
  "liked": true,
  "points_awarded": 1  // 首次 +1，重复 +0
}
```
前端只在 `points_awarded > 0` 时显示积分提示。

### 2. 老数据迁移
如果已有推荐帖子处于 `recommended = true`，建议执行迁移脚本：
```sql
INSERT INTO point_transactions (id, user_id, source, delta, balance_after, reason, idempotency_key, created_at)
SELECT 
  gen_random_uuid(),
  p.author_id,
  'recommend',
  0,
  u.points_balance,
  '历史推荐记录',
  'post:recommend:' || p.id,
  p.recommended_at
FROM posts p
JOIN users u ON u.id = p.author_id
WHERE p.recommended = true
ON CONFLICT (idempotency_key) DO NOTHING;
```
防止重构上线后，管理员取消再推荐会重复发放 +20。

### 3. 扩展示例

**签到积分**（未来）
```go
// 每日签到与点赞、发帖共享每日 +1 额度
if err := awardDailyPointTx(r.Context(), tx, user.ID, "checkin", "每日签到", 1); err != nil {
```

**精华帖奖励**（未来）
```go
// 帖子被加精，作者 +50，每个帖子终身一次
if err := awardPointsTx(r.Context(), tx, authorID, "featured", "帖子加精", "post:featured:"+postID, 50, 0); err != nil {
```

## 文件清单

### 修改的文件
- `server/internal/api/point_rewards.go` - 新增 `awardDailyPointTx` 函数
- `server/internal/api/interactions.go` - 点赞改用每日积分
- `server/internal/api/post.go` - 发帖改用每日积分
- `server/internal/api/api_test.go` - 更新测试 mock

### 新增的文件
- `server/internal/api/point_rewards_daily_test.go` - 每日积分测试

### 文档
- `积分系统实现总结.md` - 实现细节
- `积分系统检查清单.md` - 验证清单
- `积分系统统一账本实现完成.md` - 本文档

---

**实现完成时间**: 2026-09-01  
**测试状态**: ✅ 全部通过  
**编译状态**: ✅ 成功
