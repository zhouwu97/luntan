# 积分系统统一实现检查清单

## ✅ 已完成

### 1. 核心函数
- [x] `awardDailyPointTx` - 每日积分统一入口
- [x] `awardPointsTx` - 通用积分发放（推荐用）
- [x] 查询 `Asia/Shanghai` 时区日期

### 2. 业务集成
- [x] 点赞帖子 → `awardDailyPointTx`
- [x] 点赞评论 → `awardDailyPointTx`
- [x] 发布帖子 → `awardDailyPointTx`
- [x] 推荐帖子 → `awardPointsTx` (幂等 key: `post:recommend:帖子ID`)

### 3. 测试覆盖
- [x] `TestDailyPointCompetition` - 验证两个入口竞争每日额度
- [x] `TestRecommendationBonusIsIdempotentPerPost` - 验证推荐按帖子幂等
- [x] `TestDailyPointReset` - 验证第二天重置
- [x] 更新 `TestCreatePostWritesRevisionAndIsIdempotentByUserKey`
- [x] 更新 `TestPostLikeIsIdempotentAndUpdatesCountOnlyAfterNewRelation`

### 4. 积分规则
| 行为 | 积分 | 幂等 Key | 说明 |
|-----|------|---------|------|
| 点赞帖子/评论 | +1 | `daily:日期:user:用户ID` | 共享每日额度 |
| 发布帖子 | +1 | `daily:日期:user:用户ID` | 共享每日额度 |
| 推荐帖子 | +20 | `post:recommend:帖子ID` | 终身一次 |

## 🔍 验证结果

### 测试通过
```bash
# 新增测试
✅ TestDailyPointCompetition
✅ TestRecommendationBonusIsIdempotentPerPost  
✅ TestDailyPointReset

# 更新测试
✅ TestCreatePostWritesRevisionAndIsIdempotentByUserKey
✅ TestPostLikeIsIdempotentAndUpdatesCountOnlyAfterNewRelation

# 整体测试
✅ go test ./internal/api
```

### 编译通过
```bash
✅ go build ./...
```

## 📊 代码改动

### `server/internal/api/point_rewards.go`
- 新增 `awardDailyPointTx` 函数
- 查询 Asia/Shanghai 时区日期生成幂等 key

### `server/internal/api/interactions.go`
- 点赞帖子：`awardPointsTx` → `awardDailyPointTx`
- 点赞评论：`awardPointsTx` → `awardDailyPointTx`

### `server/internal/api/post.go`
- 发帖：`awardPointsTx` → `awardDailyPointTx`

### `server/internal/api/recommendations.go`
- 推荐：保持 `awardPointsTx`，幂等 key 已经是 `post:recommend:帖子ID`

### `server/internal/api/point_rewards_daily_test.go`
- 新增三个测试用例

### `server/internal/api/api_test.go`
- 更新两个测试用例的 mock 期望

## 🎯 设计优势

1. **统一收口** - 所有积分通过 `awardDailyPointTx` 或 `awardPointsTx`
2. **幂等保证** - 使用 `idempotency_key` 唯一约束
3. **事务一致性** - 流水和余额在同一事务
4. **时区正确** - 按 Asia/Shanghai 计算业务日期
5. **扩展性强** - 未来签到、精华等直接复用

## 📝 后续建议

### 短期（可选）
- [ ] 前端显示 `points_awarded` 字段（首次才显示 +1 提示）
- [ ] 积分明细页面展示流水

### 中期（推荐）
- [ ] 签到功能复用 `awardDailyPointTx`
- [ ] 精华帖奖励（幂等 key: `post:featured:帖子ID`）
- [ ] 管理员手动调整积分

### 长期（扩展）
- [ ] 积分商城兑换（扣减积分）
- [ ] 积分排行榜
- [ ] 活动期间双倍积分

## ⚠️ 注意事项

1. **迁移旧数据**：如果已有推荐帖子处于 `recommended = true`，需要初始化幂等记录防止重复发放
2. **时区一致性**：务必使用 Asia/Shanghai 计算业务日期
3. **前端体验**：首次才显示 +1 提示，避免重复点赞误导用户
4. **监控告警**：关注 `point_transactions` 插入失败率
